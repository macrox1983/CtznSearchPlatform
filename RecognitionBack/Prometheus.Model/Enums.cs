using System;
using System.Collections.Generic;
using System.Text;

namespace Prometheus.Model
{
    public enum Roles
    {
        Applicant = 1,
        Volonteur = 2,
    }

    public enum SearchTicketStatuses
    {
        Active = 1,
        Finished = 2
    }

    public enum ParticipantSearchStatuses
    {

    }
}
